# Domain Rules — [Rule Set Name]

<!--
Rules files are loaded automatically by Claude Code when present in .claude/rules/.
Use them for domain-specific constraints that apply to a subset of the project
(e.g. API design rules, database rules, security rules).

Rename this file and fill in the sections below.
-->

## When These Rules Apply

These rules apply whenever working on [describe the domain — e.g., "API endpoints", "database migrations", "frontend components"].

---

## Rules

### [Rule Category 1]

- [Specific rule — e.g., "All API responses must use the envelope format: { data, error, meta }"]
- [Specific rule — e.g., "Never return stack traces in production error responses"]
- [Specific rule — e.g., "All endpoints must validate input at the boundary before touching business logic"]

### [Rule Category 2]

- [Specific rule]
- [Specific rule]

### Never Do

- [Hard prohibition — e.g., "Never use raw SQL strings — always use the ORM or parameterized queries"]
- [Hard prohibition]

---

## Examples

### Correct

```python
# Show the right pattern
```

### Incorrect

```python
# Show what to avoid and why
```
